import type { ArgusAnalysisResult } from '../../types'

export function SuspiciousRegions({ result }: { result: ArgusAnalysisResult | null }) { return <section className="panel"><div className="section-kicker">LOCALIZATION</div><div className="panel-heading"><h2>Suspicious regions</h2>{result && <span className="panel-note">{result.suspicious_regions.length} FOUND</span>}</div>{!result || result.suspicious_regions.length === 0 ? <p className="muted">No connected suspicious regions above the current area threshold.</p> : <div className="region-table"><div className="region-header"><span>REGION</span><span>LOCATION</span><span>AREA</span><span>TRUST / ANOMALY</span></div>{result.suspicious_regions.map(region => <div className="region-row" key={region.region_id}><b>R{region.region_id}</b><span>{region.x},{region.y} · {region.width}×{region.height}</span><span>{region.area_percentage.toFixed(1)}%</span><span><em>{region.trust_score.toFixed(2)}</em> / {region.anomaly_score.toFixed(2)}</span></div>)}</div>}</section> }
